
# ignore node modules,
# we'll package the bower components for now as they are included directly in the source here and pretty light weight
node_modules

# ignore the output css generated by sass, i don't think there's much use commiting this
css/site-sass-out.css.twig
